/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:["utfs.io","githubusercontent.com", "lh3.googleusercontent.com","avatars.githubusercontent.com"]
    }
};

export default nextConfig;
